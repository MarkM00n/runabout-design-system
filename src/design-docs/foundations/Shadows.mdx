import { Meta } from '@storybook/addon-docs/blocks';
import { FoundationPage, FoundationSection } from '../FoundationPage';
import foundationsData from '../foundations-data.generated.json';

<Meta title="Foundations/Shadows" />

<FoundationPage
  title="Shadows"
  description="Elevation/shadow tokens, generated the same way as every other Foundation page. Currently empty — see the note below rather than a fabricated set of values."
>
  <FoundationSection category="shadow" tokens={foundationsData.shadow} emptyStateNote={foundationsData.shadowNote} />
</FoundationPage>
