import { Meta } from '@storybook/addon-docs/blocks';
import { FoundationPage, FoundationSection } from '../FoundationPage';
import foundationsData from '../foundations-data.generated.json';

<Meta title="Foundations/Typography" />

<FoundationPage
  title="Typography"
  description="Named type styles — font family, size, weight, line height, and letter spacing — from tokens.css/tokens.json, with a live rendered sample per style."
>
  <FoundationSection category="typography" tokens={foundationsData.typography} />
</FoundationPage>
