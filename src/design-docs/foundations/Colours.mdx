import { Meta } from '@storybook/addon-docs/blocks';
import { FoundationPage, FoundationSection } from '../FoundationPage';
import foundationsData from '../foundations-data.generated.json';

<Meta title="Foundations/Colours" />

<FoundationPage
  title="Colours"
  description="Semantic tokens (purpose-named — action-primary, border-default, etc.) with their resolved value and which components reference them, plus the full primitive palette ramps underneath (Figma's Primitives collection, 71 raw colors across 8 families). Generated by npm run design-sync — editing this page directly won't stick."
>
  <FoundationSection category="color" tokens={foundationsData.color} />
</FoundationPage>
