# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-dashboard
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Claude Code local tooling (settings, worktrees) — not project source.
# Skills are the exception: they're project-level workflow definitions
# meant to be shared and checked in, not local machine state.
.claude/*
!.claude/skills/
!.claude/skills/**
